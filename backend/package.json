{
  "name": "metresurmesure-backend",
  "version": "1.0.0",
  "description": "Backend for metresurmesure application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "render-start": "node server.js",
    "test": "echo 'Build completed - no compilation needed for Node.js'",
    "build": "echo 'No build step needed'",
    "compile": "babel src -d dist --copy-files",
    "render-postbuild": "npm run migrate",
    "render-build": "npm install",
    "clean": "rm -rf dist || true"
  },
  "dependencies": {
    "stripe": "^12.18.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.10.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.6.1",
    "nodemailer": "^6.9.1",
    "dotenv": "^16.0.3",
    "express-validator": "^6.15.0",
    "passport": "^0.6.0",
    "passport-jwt": "^4.0.1"
    
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "@babel/cli": "^7.22.0",
    "@babel/core": "^7.22.0",
    "@babel/preset-env": "^7.22.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}